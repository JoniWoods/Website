# OS
.DS_Store

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env
.env*

# Node/Next in app workspace (keep root _next static assets tracked)
node_modules/
app/node_modules/
app/.next/
app/out/
app/dist/

# Build artifacts that should typically be excluded
# Note: Keeping _next/ tracked for static hosting deployment
# but consider excluding if using a build step in deployment
*.tsbuildinfo

# IDE
.vscode/
.idea/
